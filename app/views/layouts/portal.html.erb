<!DOCTYPE html>
<html>
  <head>
    <title>Lista</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <div class="grid-container">
      <nav class="navbar bg-light">
        <div class="container">
        <a class="navbar-brand" href="/">
          <%= image_tag('logo.png', size: "60") %>
        </a>
        <% if current_user %>
          <div class="dropdown">
              <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown">
                <%= image_tag(current_user.image ?  current_user.image : 'https://caruarucity.com.br/wp-content/uploads/2016/12/avatar-vazio.jpg', size: "30", class: "avatar")%>
                <%= current_user.name %>
              </a>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                <%= link_to 'Sair', destroy_user_session_path, :method => :delete, class: 'dropdown-item text-danger' %>
              </div>
           </div>
          <% end %>
          </div>
        </nav>
        <% if current_user%>
          <div class="container">
            <div class="background">
              <div class="col-md-3 col-xs-12">
               <div class="header-perfil">
                 <div class="circle">
                  <%= image_tag('perfil.jpg', class: "avatar-perfil")%>
                 </div>
               </div>
              </div>
            </div>
            <div class="header-bottom">
              <div class="row" style="padding: 5px">
                <div class="col-md-2">
                  <a href="#" class="btn btn-lista">Imprimir Lista</a>
                </div>
                <div class="col-md-7 col-xs-12">
                  <small>
                     <strong> Local: </strong>Av. Comendador Thomaz Fortunato Nº 2000, Prai dos Namorados - Americana <br />
                     <strong> Data do Evento: </strong> 15 de dezembro de 2019
                  </small>
                </div>
                <div class="col-md-3">
                  <ul class="nav navbar-nav list-car pull-right" style="margin-right:5px;">
                   <li class="active cart">
                     <%= render 'layouts/cart' %>
                   </li>
                  </ul>
                </div>
              </div>
            </div>
            <h1 class="title">Lista do Chá da Ray</h1>
            <hr />
            <div class="row">
               <%= yield %>
             </div>
           </div>
          <% else %>
             <%= yield %>
          <% end %>
      </div>
  </body>
</html>
